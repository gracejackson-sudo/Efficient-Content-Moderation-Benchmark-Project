# Efficient-Content-Moderation-Benchmark-Project
I trained an AI model to detect hate speech on social media by only updating 1.5% of its parameters using small adapter layers (instead of retraining the whole model), then compared three different compression techniques to find the best balance of speed, size, and accuracy. Inspired by quantization training research I did while consulting Adobe.
